#version 410 core

layout( location = 0 ) in vec3 vPosition;
layout( location = 1 ) in vec4 vColor;

out vec4 color;

void
main()
{
    gl_Position = vec4(vPosition, 1); // w needs to be 1.
    color = vColor;
}
